#configuracion de acceso a BD
spring:
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update #create-drop #update
#estrategia de generacion de base de datos
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost/dbBank?useSSL=false 
    username: root
    password: root

server:
  port: 8090 
  